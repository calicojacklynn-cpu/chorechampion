{
  "entities": {
    "ParentProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ParentProfile",
      "type": "object",
      "description": "Represents a parent user profile within the Chore Champion application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ParentProfile entity."
        },
        "email": {
          "type": "string",
          "description": "Email address of the parent.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "First name of the parent."
        },
        "lastName": {
          "type": "string",
          "description": "Last name of the parent."
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName"
      ]
    },
    "ChampionProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChampionProfile",
      "type": "object",
      "description": "Represents a child (champion) profile within the Chore Champion application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ChampionProfile entity."
        },
        "parentId": {
          "type": "string",
          "description": "Reference to ParentProfile. (Relationship: ParentProfile 1:N ChampionProfile)"
        },
        "name": {
          "type": "string",
          "description": "First name of the champion."
        },
        "username": {
          "type": "string",
          "description": "The champion's username."
        },
        "email": {
          "type": "string",
          "description": "The champion's email address.",
          "format": "email"
        },
        "points": {
          "type": "number",
          "description": "Total reward points earned by the champion."
        },
        "avatarUrl": {
          "type": "string",
          "description": "URL for the champion's avatar image.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "parentId",
        "name",
        "username",
        "email",
        "points"
      ]
    },
    "Chore": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Chore",
      "type": "object",
      "description": "Represents a chore that can be assigned to a champion.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Chore entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the chore."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the chore."
        },
        "pointsValue": {
          "type": "number",
          "description": "Number of reward points awarded for completing the chore."
        },
        "preset": {
          "type": "boolean",
          "description": "Indicates whether the chore is a preset or custom chore."
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "pointsValue",
        "preset"
      ]
    },
    "AssignedChore": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AssignedChore",
      "type": "object",
      "description": "Represents a chore assigned to a specific champion on a specific date.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AssignedChore entity."
        },
        "championId": {
          "type": "string",
          "description": "Reference to ChampionProfile. (Relationship: ChampionProfile 1:N AssignedChore)"
        },
        "choreId": {
          "type": "string",
          "description": "Reference to Chore. (Relationship: Chore 1:N AssignedChore)"
        },
        "dueDate": {
          "type": "string",
          "description": "The date the chore is due.",
          "format": "date-time"
        },
        "completed": {
          "type": "boolean",
          "description": "Indicates whether the chore has been completed."
        }
      },
      "required": [
        "id",
        "championId",
        "choreId",
        "dueDate",
        "completed"
      ]
    },
    "Reward": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Reward",
      "type": "object",
      "description": "Represents a reward that can be redeemed by a champion.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Reward entity."
        },
        "parentId": {
          "type": "string",
          "description": "Reference to ParentProfile. (Relationship: ParentProfile 1:N Reward)"
        },
        "name": {
          "type": "string",
          "description": "Name of the reward."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the reward."
        },
        "pointsRequired": {
          "type": "number",
          "description": "Number of points required to redeem the reward."
        }
      },
      "required": [
        "id",
        "parentId",
        "name",
        "description",
        "pointsRequired"
      ]
    },
    "RedeemedReward": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "RedeemedReward",
      "type": "object",
      "description": "Represents a reward that has been redeemed by a champion.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the RedeemedReward entity."
        },
        "championId": {
          "type": "string",
          "description": "Reference to ChampionProfile. (Relationship: ChampionProfile 1:N RedeemedReward)"
        },
        "rewardId": {
          "type": "string",
          "description": "Reference to Reward. (Relationship: Reward 1:N RedeemedReward)"
        },
        "redemptionDate": {
          "type": "string",
          "description": "The date the reward was redeemed.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "championId",
        "rewardId",
        "redemptionDate"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "ParentProfile",
          "schema": {
            "$ref": "#/backend/entities/ParentProfile"
          },
          "description": "Stores parent user profiles. Path-based ownership ensures that only the authenticated user (parent) can access their profile.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the parent user."
            }
          ]
        }
      },
      {
        "path": "/champions/{championId}",
        "definition": {
          "entityName": "ChampionProfile",
          "schema": {
            "$ref": "#/backend/entities/ChampionProfile"
          },
          "description": "Top-level collection for champion profiles. Accessible by the champion and their parent.",
          "params": [
            {
              "name": "championId",
              "description": "The unique ID of the champion (matches their auth UID)."
            }
          ]
        }
      },
      {
        "path": "/champions/{championId}/assignedChores/{assignedChoreId}",
        "definition": {
          "entityName": "AssignedChore",
          "schema": {
            "$ref": "#/backend/entities/AssignedChore"
          },
          "description": "Stores chores assigned to a specific champion. Accessible by both champion and parent.",
          "params": [
            {
              "name": "championId",
              "description": "The unique ID of the champion profile."
            },
            {
              "name": "assignedChoreId",
              "description": "The unique ID of the assigned chore."
            }
          ]
        }
      },
      {
        "path": "/champions/{championId}/redeemedRewards/{redeemedRewardId}",
        "definition": {
          "entityName": "RedeemedReward",
          "schema": {
            "$ref": "#/backend/entities/RedeemedReward"
          },
          "description": "Stores rewards redeemed by a specific champion. Accessible by both champion and parent.",
          "params": [
            {
              "name": "championId",
              "description": "The unique ID of the champion profile."
            },
            {
              "name": "redeemedRewardId",
              "description": "The unique ID of the redeemed reward."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/rewards/{rewardId}",
        "definition": {
          "entityName": "Reward",
          "schema": {
            "$ref": "#/backend/entities/Reward"
          },
          "description": "Stores rewards created by the parent. Path-based ownership allows only the parent to access the rewards they created.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the parent user."
            },
            {
              "name": "rewardId",
              "description": "The unique ID of the reward."
            }
          ]
        }
      },
      {
        "path": "/chores/{choreId}",
        "definition": {
          "entityName": "Chore",
          "schema": {
            "$ref": "#/backend/entities/Chore"
          },
          "description": "Stores preset chores available to all users. Accessible to all users, but write access is restricted to admin roles (if implemented).",
          "params": [
            {
              "name": "choreId",
              "description": "The unique ID of the chore."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore data structure has been revised to solve a critical access control issue for champion users. Originally, champion data was nested under a parent's user document (`/users/{userId}/champions/{championId}`), which made it impossible for a logged-in champion to access their own data without knowing their parent's ID. The new structure promotes the `champions` collection to the top level (`/champions/{championId}`). This allows a champion to directly and securely access their own profile and subcollections using their own UID. The parent-child relationship is maintained via a `parentId` field within each champion document, enabling parents to query for all their children. This model supports secure and efficient access for both parents and champions without compromising data ownership principles."
  }
}